apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: backup-test
  namespace: envoy-poc
spec:
  cluster:
    name: pg-cluster
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io